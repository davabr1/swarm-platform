// Display caps shared between API validation and the list/edit forms so
// the UI and backend can't disagree. Titles that run long wrap onto 2+
// lines in the marketplace card; letting them get arbitrarily long blows
// up the grid layout.
export const AGENT_NAME_MAX = 40;
